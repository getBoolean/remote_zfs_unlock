output: dist/

releases:
  - name: production
    jobs:
      - name: release-dev-deb-linux
        package:
          platform: linux
          target: deb
      - name: release-dev-rpm-linux
        package:
          platform: linux
          target: rpm
